{
  "name": "portfolio",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "concurrently -n vite,api -c magenta,cyan \"npm:vite\" \"npm:server\"",
    "vite": "vite",
    "server": "node --watch --env-file-if-exists=.env server/index.js",
    "build": "vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "concurrently": "^10.0.3",
    "playwright": "^1.60.0",
    "vite": "^8.0.12"
  },
  "dependencies": {
    "@fastify/rate-limit": "^11.0.0",
    "fastify": "^5.8.5",
    "lenis": "^1.3.23",
    "resend": "^6.12.4",
    "three": "^0.184.0"
  }
}
